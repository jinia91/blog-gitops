apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blog-backend.fullname" . }}-test-connection"
  labels:
    {{- include "blog-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blog-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
