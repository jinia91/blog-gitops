apiVersion: v1
kind: Service
metadata:
  name: {{ include "monolith-helm.fullname" . }}
  labels:
    {{- include "monolith-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monolith-helm.selectorLabels" . | nindent 4 }}
