apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monolith-helm.fullname" . }}-test-connection"
  labels:
    {{- include "monolith-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monolith-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
